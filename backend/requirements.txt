fastapi>=0.109
uvicorn[standard]>=0.27
psycopg2-binary>=2.9
pydantic>=2.0
python-jose[cryptography]>=3.3
python-dateutil>=2.8
bcrypt>=4.0
slowapi>=0.1.9
ddgs>=9.13
pytest>=9.0
httpx>=0.28
ruff>=0.6
# Error telemetry. Wired in app/sentry_setup.py; only initializes when
# SENTRY_DSN is set in the environment, so dev runs don't phone home.
sentry-sdk[fastapi]>=2.0
# Cloudflare R2 client (S3-compatible). Wired in app/r2_storage.py but
# not yet called from any route — PR-A2b switches admin uploads to R2,
# PR-A2c migrates existing https: rows. Added in advance so the import
# is resolvable when the routes start using it.
boto3>=1.34
