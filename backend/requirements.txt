fastapi>=0.109
uvicorn[standard]>=0.27
psycopg2-binary>=2.9
pydantic>=2.0
python-jose[cryptography]>=3.3
python-dateutil>=2.8
bcrypt>=4.0
slowapi>=0.1.9
ddgs>=9.13
pytest>=9.0
httpx>=0.28
ruff>=0.6
# Error telemetry. Wired in app/sentry_setup.py; only initializes when
# SENTRY_DSN is set in the environment, so dev runs don't phone home.
sentry-sdk[fastapi]>=2.0
# Cloudflare R2 client (S3-compatible). Live since PR-A2b (#153):
# exercise_routes.add_image + bulk_images call R2Storage.put_object
# when config.r2_configured(); backfill_exercise_images.py --storage=r2
# migrates old https: rows.
boto3>=1.34
